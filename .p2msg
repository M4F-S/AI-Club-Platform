polish(P2): hero social-proof chip + agent-panel focus trap & aria-live

Final items of the Claude Opus polish & improvement plan (AgentRouter review).
Completes P0 + P1 + P2 = full plan.

P2-9 SOCIAL PROOF
- Added a hero chip beside the CTA: "N+ 42 Berliners are already in the network",
  with avatar dots + glass styling matching the design system.
- Wired to live /api/stats members count (same source as the ticker).
- Verified: renders "22+ 42 Berliners are already in the network" from API.

P2-10 AGENT-PANEL A11Y
- Added role=log + aria-live=polite + aria-relevant=additions to the agent message
  body so screen readers announce replies (index inline + v4-agent.js on
  members/event.html).
- Added a focus trap: with the panel open, Tab/Shift+Tab cycle within the panel's
  focusable controls (no keyboard escape), Escape still closes.
- Verified: Tab from last control wraps to first, aria-live present, 0 JS errors.

Full plan complete (P0 flat -> P1 -> P2). Verified all pages: 1 stylesheet, valid
JSON-LD, fonts load, 0 JS errors, no external CDN.
Commit chain: ff05bcd -> f62766d -> d9ea86e (P0) -> 1b29c5a (P1) -> this (P2).
